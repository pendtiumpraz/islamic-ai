#!/bin/sh

# Run lint-staged for ESLint
npx lint-staged

# Run TypeScript check
echo "Running type check..."
npx tsc --noEmit

if [ $? -ne 0 ]; then
  echo "TypeScript errors found. Commit aborted."
  exit 1
fi

echo "All checks passed!"
